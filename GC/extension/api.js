// Placeholder API function for scanning text
// Modular: import and use in popup.js
export async function sendToApi(text) {
  // Replace 'YOUR_API_KEY' and endpoint with your own
  // This is a dummy implementation
  // Example: const response = await fetch('https://your-api.com/scan', { ... });
  return `Scanned text length: ${text.length} (API key: YOUR_API_KEY)`;
}
